""" dayshaper.web.models

    This module defines the database tables used by the "web" application.

    Note that this module is deliberately empty, as the "web" application does
    not define any database models of its own.
"""

# This module is deliberately empty.
